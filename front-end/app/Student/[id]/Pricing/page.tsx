import Pricing from "@/app/components/Pricing"; // Make sure this path points to your component

export default function StudentPricingPage() {
  return (
    <div className="w-full bg-[#fafafb] min-h-screen">
      <Pricing />
    </div>
  );
}